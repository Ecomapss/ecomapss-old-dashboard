import { ToolbarEcoDirective } from './toolbar-eco.directive';

describe('ToolbarEcoDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolbarEcoDirective();
    expect(directive).toBeTruthy();
  });
});
