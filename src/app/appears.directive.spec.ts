import { AppearsDirective } from './appears.directive';

describe('AppearsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearsDirective();
    expect(directive).toBeTruthy();
  });
});
